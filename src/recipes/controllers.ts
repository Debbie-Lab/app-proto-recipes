/**
 * Schema →
 * {
 *    urls: [],
 *    methods: [],
 *    middlewares: [],
 *    controller: async (ctx, next) => {},
 * }
 */
import App from '../index'

export default async function (app: App) {
  console.log('debug')
}
