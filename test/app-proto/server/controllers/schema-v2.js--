import url form 'ap/url';

@url(['schema-v2'])
@method(['POST'])
@middlewares(['schema-logger'])
@post
class Schema {

  @post
  async grtrt(ctx, next) {
    ctx.ssr(tpl, tplData, pages)
    ctx.html(tpl, tplData, pages)
    ctx.json()
  }

  @get
  async contrtrtoller(ctx, next) {
    ctx.ssr(tpl, tplData, pages)
    ctx.html(tpl, tplData, pages)
    ctx.json()
  }

  @url('/schema')
  @middlewares
  async controlltrter(ctx, next) {
    ctx.ssr(tpl, tplData, pages)
    ctx.html(tpl, tplData, pages)
    ctx.json()
  }
}

export Schema;